#include "main.h"
void free_list(list_t *list)
void free_info(info_t *info, int n)
{
	if (n = 1)
	{
		free_list(info->env);
		free(info->path);
	}
	for (i = 0; argv[i]; i++)
		free(argv[i]);
	free(argv);
	free(arg);
	return;
}
